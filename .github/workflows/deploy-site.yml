# Handbook site build + GitHub Pages deploy (spec §4 Site architecture).
#
# Two jobs, one build path:
#   - pull_request touching site/** → build job only. Docusaurus fails the
#     build on broken internal links, so this doubles as the PR link-check gate.
#   - push to main touching site/** → build job uploads the Pages artifact,
#     then the deploy job publishes via the official actions/deploy-pages flow.
#
# Site URL: https://watsonwblair.github.io/lab-os/
# Requires Pages enabled with source "GitHub Actions" (Settings → Pages).
# First push-to-main deploy will fail until Pages is enabled; re-run failed
# jobs after enabling. Note: the build artifact expires after 1 day — if that
# window passes, re-run ALL jobs (not just deploy) to regenerate the artifact.

name: deploy-site

on:
  push:
    branches: [main]
    paths:
      - "site/**"
      - ".github/workflows/deploy-site.yml"
  pull_request:
    paths:
      - "site/**"
      - ".github/workflows/deploy-site.yml"

permissions:
  contents: read

# Production deploys (push) share a single 'pages' group and are never
# cancelled — one deployment at a time, no interruption mid-publish.
# PR builds get per-ref groups so they don't serialize behind deploys;
# a newer push to the same ref supersedes the queued build.
concurrency:
  group: ${{ github.event_name == 'push' && 'pages' || format('pages-{0}', github.ref) }}
  cancel-in-progress: ${{ github.event_name != 'push' }}

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: site
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: site/package-lock.json

      - name: Install dependencies
        run: npm ci

      # Fails on broken internal links — this is the PR-time gate.
      - name: Build site
        run: npm run build

      # Gated to the deploy path: configure-pages errors when Pages is not
      # yet enabled, which would fail PR builds before first enablement.
      - name: Configure Pages
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/build

  deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
